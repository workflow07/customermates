---
title: Self-Hosting
description: Customermates auf eigener Infra betreiben. Wann Self-Host sinnvoll ist, wie du es mit Docker Compose in unter 15 Minuten installierst, und wie du es betreibst.
---

Self-Hosting ist ein `docker compose up -d`, sobald du ein paar Env-Variablen gesetzt hast. Diese Seite deckt die Entscheidung vs Cloud, die Installation und den Tagesbetrieb (Update, Restart, Reset, Backup) ab.

## Self-Host vs Cloud

Beide haben denselben Funktionsumfang.

| Wenn du… | Nimm |
|---|---|
| Heute noch mit dem CRM arbeiten willst | **Cloud** |
| Ein Team von < 20 ohne harte Data-Residency-Anforderung | **Cloud** |
| Das CRM auf Private-Infra für Compliance brauchst | **Self-Host** |
| Long-term weg von Pro-Seat-Pricing willst | **Self-Host** |
| Agentur mit mehreren Mandanten bist | **Self-Host**, eine Instanz pro Mandant |
| Zum Projekt beitragen willst | **Self-Host** lokal, Cloud für echte Arbeit |

| | Cloud | Self-Host |
|---|---|---|
| Setup-Zeit | 2 Minuten | ~15 Minuten |
| Infra, die du managst | keine | Docker, Postgres, Proxy, TLS, Backups |
| Updates | automatisch | `selfhost-update.sh` |
| EU-gehostet | ✓ | wo du willst |
| Backups | automatisch täglich | du konfigurierst |
| Support-SLA | inkl. in Paid-Plans | Community |
| Enterprise-Features (SSO, Audit-Log) | Paid-Plan | Paid-License-Key |
| Pricing | pro Sitz | kostenlos, nur Enterprise-Lizenz zahlen |

**Datenhoheit:** Die Cloud speichert Daten in unserer EU-Region, DSGVO-konform, mit üblicher Verschlüsselung at-rest und in-transit. Wenn deine Compliance verlangt, dass Daten auf deiner Infra liegen: Self-Host.

**Kostenform:** Cloud ist vorhersagbar pro Sitz und linear skalierend. Self-Host ist kostenlos für den Core plus was du für Infra zahlst (ein kleiner VPS trägt Hunderte User). Für ein 5er-Team ist Cloud meist günstiger, wenn du die Zeit fürs eigene Postgres-Backup einrechnest. Für ein 50er-Team lohnt sich Self-Host schnell.

Du kannst zwischen beiden wechseln. Export von einem, Import ins andere. Identisches Datenmodell. Kein Lock-in in beide Richtungen.

## Installation

### Voraussetzungen

- Docker und Docker Compose v2.
- Domain-Name, falls du TLS willst (lokal nicht nötig).
- ~2 GB RAM und ein paar GB Disk pro tausend Records.

### 1. Clonen und konfigurieren

```bash
git clone https://github.com/customermates/customermates.git
cd customermates
./scripts/selfhost-setup.sh
```

Das Setup-Script kopiert `.env.selfhost.template` nach `.env`. Die wichtigen Werte editieren:

- `BETTER_AUTH_SECRET`: langer Zufallsstring (`openssl rand -hex 32`).
- `CRON_SECRET`: ein weiterer Zufallsstring.
- `POSTGRES_PASSWORD`: default ändern.
- `BASE_URL`: deine Public-URL (z.B. `https://crm.example.com`). Lokal default `http://localhost:4000`.
- `RESEND_API_KEY` und `RESEND_FROM_EMAIL`: nötig für Signup- und Einladungs-Mails.

### 2. Starten

```bash
docker compose up -d
```

First-Boot dauert eine Minute, während Prisma-Migrationen laufen. Logs ansehen:

```bash
docker compose logs -f app
```

Wenn die App ready ist, `http://localhost:4000` öffnen (oder dein eigenes `APP_PORT`).

### 3. Erster Account

URL öffnen. Mit E-Mail registrieren. Weitere User und Rollen verwalten über Unternehmen → Benutzer und Unternehmen → Rollen.

### 4. Reverse-Proxy und TLS

Reverse-Proxy (Caddy, nginx, Traefik) auf den App-Port zeigen (4000 by default, oder dein eigenes `APP_PORT`). Caddy:

```text
crm.example.com {
  reverse_proxy localhost:4000
}
```

Customermates setzt Secure-Cookies, wenn `BASE_URL` mit `https://` startet. Der Proxy muss `X-Forwarded-Proto` korrekt weiterreichen.

### 5. API-Key anlegen

Profil → API Keys → Neuer Key. Genau wie in der Cloud. Siehe [API-Keys](/docs/api-keys).

## Tagesbetrieb

### Update

```bash
./scripts/selfhost-update.sh
```

Pullt das neueste App-Image, lässt `prisma migrate deploy` laufen und startet die Services `app` und `webhook-worker` neu. Status mit `docker compose ps` prüfen.

### Neu starten

```bash
./scripts/selfhost-restart.sh
```

Startet den Stack ohne Migrationen neu. Nach `.env`- oder Config-Änderungen.

### Logs und Troubleshooting

```bash
docker compose logs -f app
docker compose logs -f postgres
docker compose ps
docker compose exec app sh
```

### Alle Daten zurücksetzen

```bash
./scripts/selfhost-reset.sh
```

Verlangt explizite `RESET`-Bestätigung, bevor Volumes gelöscht werden. IRREVERSIBLE.

## Backups

Postgres per `pg_dump` auf Zeitplan. Der App-Container ist stateless.

```bash
docker compose exec -T postgres pg_dump -U "$POSTGRES_USER" "$POSTGRES_DB" \
  | gzip > /var/backups/customermates-$(date +%Y%m%d).sql.gz
```

Für Produktion:

- Tägliche Dumps auf separates Volume oder Off-Site.
- Restore-Prozeduren in einer Nicht-Produktion testen.
- `.env` und Secrets nicht ins Source-Repo.

## Enterprise-Features

Audit-Logging, SSO und White-Labeling brauchen eine Enterprise-Lizenz. Alles andere ist kostenlos. Lizenz gemäß der Anleitung konfigurieren, die mit deinem Lizenzschlüssel geliefert wird.

## Weiter

- [Architektur und Sicherheit](/docs/architecture-security): was du betreibst.
- [API-Keys](/docs/api-keys): Key-Hygiene.
- [KI verbinden](/docs/mcp-connect-claude-desktop): nach dem Install einen MCP-Client auf deine Instanz zeigen.
