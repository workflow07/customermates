---
title: Connect Codex
description: Run Customermates as an MCP server inside the Codex CLI. One TOML block, paste the setup prompt.
---

OpenAI's Codex CLI uses a TOML config file. The `codex mcp add` subcommand only handles stdio servers, so for an HTTP MCP server like Customermates we add the block manually. It's still one paste.

## 1. Create an API key

In Customermates: **Profile → API Keys → New key**. Name it for the client (e.g. `Codex`). Copy the 64-character string immediately — it's shown once.

## 2. Add the server to your Codex config

Open `~/.codex/config.toml` (create it if missing). Append, replacing `YOUR_KEY`:

<McpInstallSnippet tool="codex" />

Self-hosting? Swap the URL for your instance.

To pull the key from your environment instead of putting it in the file:

```toml
[mcp_servers.customermates]
url = "https://customermates.com/api/v1/mcp"
env_http_headers = { "x-api-key" = "CUSTOMERMATES_API_KEY" }
```

…then export `CUSTOMERMATES_API_KEY` in your shell profile.

## 3. Paste this setup prompt

Start a new Codex session and send this as the first message:

<McpSetupPrompt />

The agent will orient itself and wait for direction.

## Try your first real prompt

- *"Move the Acme deal to stage 'Won' and add a note that the contract was signed today."*
- *"Pull the last ten contacts I created. Any without an email address?"*
- *"Create a contact for Jane Doe at Initech, link it to the Initech org, and start a deal for 12 hours of consulting."*

## Troubleshooting

| Symptom | Fix |
|---|---|
| Server not picked up | End the current Codex session and start a new one |
| "Invalid API key" | Regenerate in Profile → API Keys; ensure the full 64 chars are quoted |
| TOML parse error | Inline-table values use `=`, not `:` — double-check the `http_headers = { ... }` line |

## Next

- [MCP tool catalog](/docs/mcp-tool-catalog): every tool the agent can call.
- [Webhooks](/docs/webhooks): subscribe other systems to CRM changes.
- [Connect Claude Code](/docs/mcp-connect-claude-code): if you also use Claude Code.
